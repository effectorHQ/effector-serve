/**
 * @effectorhq/server — runtime HTTP/server bridge for typed effector tools (WIP).
 *
 * Implementation will land here; this file exists so the package has a valid entry point.
 */

export const SERVER_PACKAGE = '@effectorhq/server';
export const SERVER_VERSION = '0.1.0';
